// Reserved for exposing safe native APIs to the web app via contextBridge
// if the accounting app ever needs OS-level features (native file dialogs,
// auto-update, etc.). Empty for now — the app runs entirely as web content.
